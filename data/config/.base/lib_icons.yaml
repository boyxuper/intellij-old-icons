#author johnx
target: lib/icons.jar
source: lib_icons

actions:
  auto_replace: True #replace as directory structure
  replace:
    - ['fileTypes/css.png', 'css.png']  #first is source, relative path
